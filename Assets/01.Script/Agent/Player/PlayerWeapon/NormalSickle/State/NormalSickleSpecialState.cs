using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NormalSickleSpecialState : WeaponState<NormalSickleEnum>
{
    public NormalSickleSpecialState(Player owner, WeaponStateMachine<NormalSickleEnum> stateMachine, string animaHash) : base(owner, stateMachine, animaHash)
    {
    }

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
